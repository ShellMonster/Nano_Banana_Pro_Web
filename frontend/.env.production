# 生产环境后端 API 地址
# 构建时使用此配置

# Docker 部署：使用相对路径，由 Nginx 代理
VITE_API_URL=/api/v1

# 本地开发（可选）：
# VITE_API_URL=http://localhost:8080/api/v1

# 可选：WebSocket 地址（未配置时会从 VITE_API_URL 自动推导为 wss://...）
# VITE_WS_URL=/api/v1
